apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend
spec:
  tls:
    - secretName: tls-secret
  defaultBackend:
    service:
      name: backend
      port:
        name: api-port
