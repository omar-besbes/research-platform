apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
spec:
  tls:
    - secretName: tls-secret
  defaultBackend:
    service:
      name: frontend
      port:
        name: api-port
