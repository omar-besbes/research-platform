apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-wrapper
spec:
  tls:
    - secretName: tls-secret
  defaultBackend:
    service:
      name: go-wrapper
      port:
        name: api-port
